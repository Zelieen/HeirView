[project]
name = "heirview"
version = "0.1.0"
description = "Plots a family tree from .gedcom"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
    "ruff>=0.16.7",
]
